{
  "name": "squadbox",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered web application builder - Vite React Project",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "vercel-build": "echo 'Building Vite React project' && vite build",
    "build:production": "echo '🚀 Production build with optimizations' && vite build --mode production",
    "build:staging": "echo '🔧 Staging build with debug info' && vite build --mode staging",
    "build:development": "echo '🛠️ Development build with source maps' && vite build --mode development",
    "build:preview": "echo '👀 Preview build for testing' && vite build --mode preview",
    "build:feature": "echo '✨ Feature branch build' && vite build --mode feature",
    "build:hotfix": "echo '🚨 Hotfix build with emergency fixes' && vite build --mode hotfix",
    "build:release": "echo '📋 Release candidate build' && vite build --mode release",
    "preview": "vite preview",
    "deploy": "vercel --prod",
    "clean": "rm -rf dist node_modules/.vite"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@mantine/core": "^8.2.3",
    "@mantine/form": "^8.2.3",
    "@mantine/hooks": "^8.2.3",
    "@mantine/notifications": "^8.2.3",
    "@tabler/icons-react": "^3.34.1",
    "dotenv": "^17.2.1",
    "mongodb": "^6.18.0",
    "mongoose": "^8.17.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "requests": "^0.3.0",
    "terser": "^5.43.1",
    "vercel": "^32.5.0",
    "vite": "^5.4.19"
  },
  "engines": {
    "node": ">=18"
  }
}
